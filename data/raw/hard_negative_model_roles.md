# Hard Negative Model Roles

This note is a hard negative for model-role retrieval evaluation.

It mentions Qwen, DeepSeek, local model, writer, planner, verifier, reasoning, document answer, report, inference, GPU, and serving.

The note compares model names and deployment vocabulary but does not state which model is the default writer or which model is the reasoning specialist.

It exists to test whether retrieval and reranking can avoid plausible but incomplete model-role evidence.
