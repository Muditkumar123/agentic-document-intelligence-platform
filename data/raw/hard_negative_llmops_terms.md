# Hard Negative LLMOps Terms

This note is a hard negative for LLMOps retrieval evaluation.

It repeats terms such as generation, run, record, prompt, model, profile, token, latency, citation, quality, metric, report, registry, versioning, and hash.

The note discusses the vocabulary of LLM monitoring but does not provide the full list of fields that every generation run records.

This near-match chunk should be considered less useful than the prompt registry document when a user asks for precise recorded metadata.
